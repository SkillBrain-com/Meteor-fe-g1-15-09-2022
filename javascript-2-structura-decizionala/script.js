const nume = "Pizza Americana";
const termenCautare = "Pizza Americana";
const rezultat = nume.includes(termenCautare) === true ? `Rezultatul este ${nume}` : "Nu s-au gasit rezultate";
console.log(rezultat);

